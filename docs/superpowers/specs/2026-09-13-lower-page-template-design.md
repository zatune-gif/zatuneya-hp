# 下層ページのデザインテンプレート

2026-09-13。ユーザー承認済みの「部品見本兼コピー元HTML＋本文専用CSS＋使い方」を実装する。

## 範囲

`v2/lower-page-template.html` を同じ `v2/` に複製し、不要な本文セクションを削って利用する。見本は実際のページの流れ（見出し、写真と文章、カード、料金、進め方、FAQ、問い合わせ）で構成し、管理画面風の説明ラベルを各所へ重ねない。冒頭にサンプルを明示し、部品境界はHTMLコメント、編集説明は `docs/templates/lower-page-template.md` に置く。

外枠は現行TOPのSVGロゴ・ナビ・フッター・追従診断をそのまま継承し、TOP内リンクだけ `./index.html#journey` に補正する。既存16ページ、共通CSS/JS、親gitlink、本番は変更しない。本文専用 `lower-page-template.css` のセレクタは `.lp-*` に隔離し、`top-comp.css` の後に読む。

## 表現・安全性

白・淡いティール・明朝見出しという承認済TOPの意匠、共有システムフォントと色・4pxトークンを使用する。カードは細枠・角丸12px・24px内側余白。料金は「料金を記載」で金額や契約を捏造しない。写真は既存仮画像で、説明に仮素材と明示。FAQはテンプレートの使い方を扱い、営業上の約束を作らない。フォームや送信処理は作らず、既存contactへ遷移する。

`noindex,follow`、自己canonical、サンプル明示、サイトナビ・sitemap非掲載。noindexはアクセス制限ではない。複製後はメタ情報、URL、画像、本文を更新し、公開前承認を得てからnoindexを外す。診断meta1個とdata-diagnosis-link委譲、既存DOMフックを保持する。

## 検証

専用静的契約を先に作り、不在でRED確認後に実装する。外枠をTOPと逐語比較（アンカー補正のみ許容）、ローカル参照・meta・サンプル・CSS隔離・既存ナビ非掲載を検証する。375/768/1280スクショ、320横溢れ、Chromium/Firefox/WebKitの375/1280、キーボードナビ・FAQ・追従表示/抑制、axeを実測する。共有コード変更なしのため全サイトの重いスイートは再走しない。root目視ゲート前にcommit/pushしない。

## 実施結果（2026-09-13）

- 専用契約：不在によるREDを確認後、149項目PASS。外枠3部、各ローカルリンク・fragment実在、CSS名前空間、noindex・既存ページ非掲載を含む。
- 専用ブラウザ：136項目PASS。Chromium320/375/768/1280、Firefox/WebKit各375/1280の8条件、axe8スキャン違反0。キーボードナビ・FAQ・focus可視、Hero/末尾で追従非表示・本文で表示、実際のIntersectionObserver発火を確認。
- 既存verify-v2：533項目PASS。375/1280全体・375追従CTAをrootが目視し承認。
- スクショ：`v2/qa-screenshots/lower-page-template/` 内の320/375/768/1280全体と375/1280追従表示、計6枚。
- 余白は0/autoと共有4pxトークン。コントラストはink/白11.44、ink/淡背景10.31、teal-dark/白5.36、teal-dark/淡背景4.83、muted/白6.10、orange-ink/橙6.90。通常文字AAを満たす。
- 最適化確認：新HTML/CSSにinline style、使い捨てログ、JSダイアログ、死にコード、秘密キー候補なし。API・エラー返り値設計は今回の静的本文に該当しない。
- 未検証：実端末、外部診断サービス内部、実際のお問い合わせ送信、複製後の実ページ。全サイトQA/Lighthouse再実行は範囲外。noindexはアクセス制御ではない。
