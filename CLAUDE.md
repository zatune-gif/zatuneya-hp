# CLAUDE.md

最初に同じディレクトリの`AGENTS.md`を読み、共通ルールの正本として従う。

Claude Codeは、親プロジェクト「暮らしの土台」と、ざつね屋事業全体の統括・整合確認を担う。通常のHP開発・保守・デザイン改善はCodexが主担当とする。

大規模変更では、Codexが用意した差分と検証結果を確認できる状態にしてから、統合を判断する。Claude Codeが直接変更する場合も、`AGENTS.md`、作業前のGit確認、サブモジュールと親の二段階Git更新、検証、Obsidian記録を遵守する。

共通ルールはこのファイルへ重複させず、Claude Code固有の補足だけを置く。
